// Solution is to use Module pattern IIFF (Immediately invoked function expression). 
// Just wrap the whole js code with an anonymous Immediately-invoked Function to remove all that variables from the Global namespace